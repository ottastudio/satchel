import React from 'react';
import { Container } from '../../hoc/global';

const Product = (props) => {
    return (
        <Container>
            Product
        </Container>
    );
};

export default Product;